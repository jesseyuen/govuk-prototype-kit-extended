const express = require('express')
const router = express.Router()

// Success message
// router.post('/examples/success-errors', function (req, res) {
//     res.render('examples/success-errors/answer', { showMessage: true })
// })

module.exports = router
